<%= render 'refinery/admin/search_header', :url => refinery.biographies_admin_biographies_path %>
<div class='pagination_container'>
  <% if @biographies.any? %>
    <%= render 'biographies' %>
  <% else %>
    <p>
      <% if searching? %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% else %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% end %>
    </p>
  <% end %>
</div>
