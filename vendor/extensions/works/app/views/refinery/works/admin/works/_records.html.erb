<%= render 'refinery/admin/search_header', :url => refinery.works_admin_works_path %>
<div class='pagination_container'>
  <% if @works.any? %>
    <%= render 'works' %>
  <% else %>
    <p>
      <% if searching? %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% else %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% end %>
    </p>
  <% end %>
</div>
